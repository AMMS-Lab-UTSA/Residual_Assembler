"""Partner-side residual-sensitivity kit.

A self-contained SDK a collaborator runs locally to generate residual-sensitivity
right-hand-sides (``R^(p)``) and solve/export ``T U^(p) = -R^(p)`` — without
sharing source code, mesh, material model, or the full residual/tangent. See
``README.md`` and ``docs/``.
"""
