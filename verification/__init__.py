"""Claim-by-claim reproduction of the quantitative statements of the project presentation.

Each ``claimN_*.py`` module reproduces one quantitative statement of the slides
from the two repositories, against an independent reference, and writes a
machine-readable result. ``run_all.py`` runs them in order. See
``docs/VERIFICATION_RECORD.md`` for the claim -> slide -> command -> reference
-> measured value table.
"""
