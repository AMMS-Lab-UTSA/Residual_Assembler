"""Optional Streamlit front end for ``resasm``.

Importing this package does not import Streamlit; only
``residual_core.app.streamlit_app`` does, so ``residual_core`` stays a
numpy-only dependency for everyone who uses the CLI or the Python API.
"""
